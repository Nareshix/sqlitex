# release.toml (at the root of your repo)
consolidate-commits = true
shared-version = true
pre-release-hook = ["git-cliff", "--unreleased", "--tag", "{{version}}", "--prepend", "CHANGELOG.md"]
pre-release-commit-message = "chore: Release version {{version}}"